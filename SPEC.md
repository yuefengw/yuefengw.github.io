# 个人主页重构规格说明

> 状态：已确认，首版已实现，后续按此文档迭代  
> 仓库：`yuefengw/yuefengw.github.io`  
> 目标域名：`www.wyf219.top`  
> 参考站：`https://www.wpironman.top/`

## 1. 结论

首版可以完整部署在 GitHub Pages，不需要购买或维护服务器。

参考站的主体也是由 GitHub Pages 托管的 Hexo 静态博客，页面中的文章、分类、标签、归档、搜索、站点字数、更新时间和响应式布局都可以在构建时生成。参考站额外使用了第三方服务提供贡献日历、访问量和评论，这些不是 GitHub Pages 自带能力。

本项目首版采用以下边界：

- GitHub Pages 负责托管全部页面和静态资源。
- GitHub Actions 负责构建博客、生成站点统计和部署。
- GitHub 贡献日历优先在 Actions 构建时生成静态数据，不依赖自建服务器。
- 评论、访问量、在线表单等动态功能不进入首版，后续可用第三方或无服务器服务补充。
- 不复制参考站的头像、背景图、文章、品牌名称等内容，只参考布局、信息层级和交互方式。

只有在以下情况下才需要额外服务：

- 需要登录、后台管理、数据库或私密内容；
- 需要自己保存评论、访问量等实时数据；
- 需要稳定的中国大陆访问速度，并愿意使用境内服务器和备案域名。

## 2. 项目现状

仓库已经拉取到本地并完成首版重构：

- 原始 `index.html`、`白底.jpg` 和 `CNAME` 仍保留在源码仓库，便于回溯；
- 新站由 Hexo + Butterfly 从 `source/` 生成，正式部署只发布 `public/` 构建产物；
- 旧主页中的个人资料、教育经历、项目、实习、技能和奖项已迁移到新的“关于”和“项目”页面；
- GitHub Actions 已配置为构建并部署 GitHub Pages，源码分支不提交 `public/`、`db.json` 和 `node_modules/`。

旧主页还公开了完整邮箱、手机号和所在地。新站默认不展示手机号，邮箱和所在地只有在确认后才公开，避免在迁移内容时无意延续敏感信息。

当前域名还有一个上线前必须处理的问题：`www.wyf219.top` 已经指向 GitHub Pages，但 HTTPS 返回的是 `*.github.io` 证书，域名证书尚未正确签发。需要在 GitHub 仓库的 Pages 设置中重新确认自定义域名，等待证书生成，再开启 `Enforce HTTPS`。

## 3. 技术方案

### 3.1 推荐技术栈

- 静态站点生成器：Hexo 7；
- 基础主题：Butterfly，固定依赖版本；
- 定制方式：独立主题配置、自定义 CSS、少量自定义 JavaScript 和 Hexo 辅助脚本；
- 内容格式：Markdown + Front Matter；
- 构建环境：Node.js LTS + npm lockfile；
- 部署方式：GitHub Actions 官方 Pages 工作流；
- 搜索：构建时生成 `search.json`，浏览器本地搜索；
- 图标：主题现有图标集或 Lucide，不手绘重复图标；
- 字体：优先系统字体或仓库内自托管字体，避免依赖 Google Fonts。

选择 Hexo + Butterfly 的原因：

- 参考站本身使用 Hexo 7.3.0 和 Butterfly，目标布局可以通过主题配置和局部定制实现；
- 分类、标签、归档、文章分页、RSS、站点地图和本地搜索已有成熟能力；
- 构建结果是纯静态文件，和 GitHub Pages 完全兼容；
- 后续新增文章只需要提交 Markdown，不需要手写页面。

### 3.2 仓库结构

当前采用以下结构：

```text
.
├── .github/workflows/deploy-pages.yml
├── scripts/
├── source/
│   ├── _posts/
│   ├── about/index.md
│   ├── projects/index.md
│   ├── images/
│   └── CNAME
├── node_modules/（本地安装，不提交）
├── _config.yml
├── _config.butterfly.yml
├── package.json
├── package-lock.json
└── SPEC.md
```

`main` 分支保存源文件，GitHub Actions 构建后直接发布 Pages artifact，不把生成后的 `public/` 目录提交到源码分支。

## 4. 页面范围

### 4.1 首页 `/`

首页打开后直接显示参考截图中的内容区，不显示参考站现有的全屏欢迎 Hero，不要求用户先滚动一屏。

桌面端首屏必须同时看到：

- 左侧个人资料卡；
- 右侧 GitHub 贡献日历卡；
- 至少一张文章摘要卡；
- 固定背景图和半透明内容面板。

首页采用两栏布局：

- 左栏：固定宽度约 `300-330px`；
- 右栏：占据剩余空间，最小宽度 `0`，避免长标题撑破布局；
- 页面内容最大宽度约 `1400px`，在超宽屏居中；
- 两栏顶部对齐，整页正常滚动；
- 左栏在桌面端可使用粘性定位，但不能造成内部滚动和内容截断。

### 4.2 文章详情 `/posts/:slug/`

包含：

- 标题、发布日期、更新时间、分类、标签和封面；
- Markdown 正文、代码高亮、引用、表格、图片和数学公式；
- 桌面端目录，移动端可折叠；
- 上一篇/下一篇；
- 返回首页、返回顶部；
- 可选分享链接；
- 首版不启用评论。

### 4.3 聚合页面

- `/archives/`：按年份和月份列出文章；
- `/categories/`：分类树和各分类文章数；
- `/tags/`：标签云和标签文章列表；
- `/about/`：个人简介、教育经历、技能、奖项和联系方式；
- `/projects/`：AstraFlow、ClaimAssist、实习项目等现有内容；
- `/search/` 或搜索弹层：本地全文搜索；
- `/404.html`：静态 404 页面；
- `/atom.xml`：RSS；
- `/sitemap.xml`：站点地图。

文章永久链接使用 `/posts/:slug/`，避免中文标题或日期变更导致 URL 改动。

## 5. 首页模块规格

### 5.1 左侧栏顺序

左侧模块按以下顺序展示：

1. 个人资料；
2. 公告；
3. 最新文章；
4. 分类；
5. 标签；
6. 归档；
7. 网站信息。

#### 个人资料

- 使用现有个人照片，实施时转为 WebP 并提供清晰的替代文本；
- 展示中文名、英文名或一句简介；
- 展示文章数、标签数、分类数；
- GitHub 主按钮指向 `https://github.com/yuefengw`；
- 展示 GitHub、CSDN 和可选邮箱等图标入口；
- 默认不展示手机号；
- 邮箱和所在地是否公开由最终配置决定。

#### 公告

- 支持标题、两到三行说明和一个可选按钮；
- 没有公告内容时整个模块不渲染，不显示空卡片；
- 首版可用于推荐最新项目或个人动态。

#### 最新文章

- 展示最近 4 篇；
- 每项包含缩略图、标题和日期；
- 标题最多显示两行；
- 没有文章时显示简洁空状态，不伪造示例文章。

#### 分类

- 展示分类名称和文章数；
- 支持最多两级层级；
- 首页最多展示 8 个顶级条目，提供“查看全部”入口。

#### 标签

- 使用可点击标签胶囊或紧凑标签云；
- 首页优先展示使用频率最高的 24 个标签；
- 全部标签在 `/tags/` 页面展示。

#### 归档

- 展示最近 8 个有文章的月份及文章数；
- 提供进入完整归档页的入口。

#### 网站信息

- 文章数目：构建时统计；
- 运行时间：根据站点发布日期在浏览器计算；
- 本站总字数：构建时统计；
- 最后更新时间：由最近一次构建或文章更新时间生成；
- 总访问量：首版不展示，避免永久处于“加载中”或依赖不稳定服务。

### 5.2 右侧内容

#### GitHub 贡献日历

- 位于首页右栏顶部；
- 展示最近 12 个月贡献热力图；
- 展示过去一年、最近一个月和最近一周的贡献数；
- 这里统计的是 GitHub contribution calendar 中的贡献，不是博客发文次数；
- 数据源用户为 `yuefengw`；
- GitHub Actions 每天定时获取数据并生成静态 JSON，优先使用工作流内置凭据；权限不足时才使用只读 fine-grained Token；
- 页面加载时只读取本站静态 JSON，不暴露 Token；
- 数据获取失败时优先读取当前线上版本的 JSON；仍不可用时直接降级，不能阻断博客部署；
- 如果没有任何可用数据，降级为 GitHub 主页链接和说明，不显示无限加载动画。

#### 文章摘要列表

- 每页默认 8 篇；
- 桌面端封面图在左右两侧交替；
- 卡片包含标题、日期、分类、标签和摘要；
- 标题最多三行，摘要最多三到四行；
- 封面缺失时使用统一默认图，不拉伸图片；
- 支持分页，直接访问分页 URL 时必须能在 GitHub Pages 正常打开。

## 6. 视觉与交互

### 6.1 视觉方向

- 使用真实背景图片覆盖视口，背景固定或使用低成本视差；
- 背景上增加遮罩，确保文字对比度；
- 内容卡片使用浅色半透明背景和适度模糊；
- 卡片圆角不超过 `8px`；
- 主色不照搬参考站，依据最终背景图选择中性色加一个强调色；
- 不使用装饰性渐变球、无意义大面积渐变或嵌套卡片；
- 文章区保持清晰、安静，避免视觉效果影响阅读。

背景图、头像和文章封面必须使用本人拥有授权的素材。首版不使用参考站的电影截图或其他版权素材。

### 6.2 顶部导航

- 首页不出现全屏 Hero；
- 桌面端使用紧凑导航，包含首页、分类、标签、归档、项目、关于和搜索；
- 导航不得挤占主要首屏，可采用透明覆盖或窄高度粘性栏；
- 移动端使用菜单图标打开抽屉；
- 所有图标按钮提供可访问名称和悬停提示。

### 6.3 响应式行为

- `>= 1200px`：完整双栏；
- `768px - 1199px`：收窄双栏，必要时减少标签数量；
- `< 768px`：单栏，不使用横向滚动。

移动端顺序：

1. 个人资料；
2. GitHub 贡献日历；
3. 文章列表；
4. 公告；
5. 最新文章；
6. 分类；
7. 标签；
8. 归档；
9. 网站信息。

贡献日历在窄屏允许水平滚动其网格，但卡片本身不能撑破页面。所有按钮、标签和标题在 `320px` 宽度下不得互相覆盖。

### 6.4 主题与动画

- 支持浅色/深色模式并记住选择；
- 默认主题根据最终背景图确定，不强制跟随系统；
- 动画只用于卡片进入、菜单和返回顶部；
- 遵守 `prefers-reduced-motion`；
- 不加入影响阅读的粒子、鼠标尾迹或持续大幅动画。

## 7. 内容模型

每篇文章使用以下 Front Matter：

```yaml
---
title: 文章标题
slug: stable-english-slug
date: 2026-08-19 12:00:00
updated: 2026-08-19 12:00:00
categories:
  - 工程实践
tags:
  - Java
  - AI
cover: /images/posts/stable-english-slug/cover.webp
description: 用于首页摘要和 SEO 的一句话说明。
draft: false
---
```

规则：

- `slug` 一经发布不随标题改变；
- `description` 必填，避免自动截取产生不完整摘要；
- 封面统一使用 WebP/AVIF；
- 草稿不进入生产构建；
- 分类最多两级，标签使用统一大小写和命名。

已将 CSDN 账号 `qq_41725967` 的 23 篇公开原创文章迁移到本站，并新增一篇 mini-pi Agent 教程。CSDN 迁移内容保留原始发布日期、修改时间、标签和原文链接，正文图片下载到仓库内自托管；新增文章按本站内容模型维护，不在每次构建时实时抓取外部平台。

当前站点已有真实文章内容，后续新增文章直接在 `source/_posts/` 中创建 Markdown，并通过稳定 slug 发布。

## 8. 动态能力边界

GitHub Pages 本身没有数据库和服务端代码，以下能力按阶段处理：

| 能力 | 首版方案 | 是否需要自建服务器 |
| --- | --- | --- |
| 分类、标签、归档 | Hexo 构建生成 | 否 |
| 本地全文搜索 | 静态 `search.json` | 否 |
| GitHub 贡献日历 | Actions 定时生成静态数据 | 否 |
| 站点字数、更新时间 | 构建时统计 | 否 |
| 评论 | 首版关闭；后续可接 Giscus | 否 |
| 访问统计 | 首版关闭；后续接隐私友好统计服务 | 否或无服务器 |
| 联系表单 | 首版使用邮箱链接 | 否 |
| 登录、后台、私密文章 | 不在范围内 | 是 |

任何 GitHub Token 或第三方密钥只能通过 GitHub Actions 的受保护凭据或 Secrets 注入，禁止写进前端代码或提交到仓库。

## 9. 部署与域名

### 9.1 自动部署

- 推送到 `main` 后触发构建；
- 安装锁定依赖；
- 执行内容校验、Hexo 构建和链接检查；
- 上传 Pages artifact；
- 使用官方 `deploy-pages` action 发布；
- 每天定时刷新一次 GitHub 贡献数据；
- 构建失败时保留上一版线上站点。
- 正式上线时同步更新 GitHub 仓库描述，移除已经过时的 Hugo 技术说明。

### 9.2 自定义域名

- 保留 `www.wyf219.top`；
- DNS 继续使用 `CNAME -> yuefengw.github.io`；
- `source/CNAME` 随构建复制到站点根目录；
- 在 GitHub 的域名验证流程中补充或核对 DNS TXT 记录，降低域名被其他仓库接管的风险；
- 在 GitHub Pages 设置中重新验证域名并等待证书签发；
- 证书正确后开启强制 HTTPS；
- 验证 `https://yuefengw.github.io/` 正确跳转到 HTTPS 自定义域名，而不是 HTTP。

### 9.3 中国大陆访问说明

GitHub Pages 技术上可用，但中国大陆网络下速度和稳定性无法保证。首版通过以下方式降低影响：

- 字体、脚本、样式和主要图片全部本站托管；
- 不把关键页面功能建立在境外公共 CDN 或第三方 API 上；
- 压缩背景图和文章封面；
- 可选使用 Cloudflare 做 DNS/CDN，但不承诺其在大陆网络稳定。

如果未来目标变为“面向大陆用户稳定、快速访问”，再评估境内对象存储/CDN或服务器，并处理备案；这不是首版前置条件。

## 10. 性能、SEO 与可访问性

### 10.1 性能预算

- 桌面背景图建议不超过 `500 KB`；
- 移动端使用单独裁切图，建议不超过 `300 KB`；
- 单张文章封面建议不超过 `200 KB`；
- 首屏之外图片使用懒加载；
- 避免加载未使用的完整图标库和大体积前端框架；
- 贡献日历失败不能拖慢文章显示。

### 10.2 SEO

- 每页唯一标题和 description；
- canonical URL；
- Open Graph 和社交分享图；
- `robots.txt`、`sitemap.xml`、RSS；
- 文章使用语义化标题结构；
- 图片包含替代文本。

### 10.3 可访问性

- 正文和背景对比度满足 WCAG AA；
- 键盘可以访问导航、搜索、主题切换和文章链接；
- 焦点样式清晰可见；
- 纯图标按钮必须有可访问名称；
- 动画可被系统“减少动态效果”设置关闭。

## 11. 验收标准

实现完成后至少满足：

- 打开首页后无需滚动即可看到个人资料、贡献日历和第一篇文章；
- 首页不存在全屏欢迎页；
- 桌面端左栏按“个人资料、公告、最新文章、分类、标签、归档、网站信息”排序；
- 分类、标签、归档、文章详情和分页都能通过直接 URL 打开；
- 在 `320px`、`768px`、`1440px` 宽度下没有文字或控件重叠；
- JavaScript 关闭时仍能浏览文章、分类、标签和归档；
- 贡献数据或第三方资源失败时，文章与导航仍可正常使用；
- 本地生产构建通过，无破损内部链接；
- GitHub Actions 可以从干净环境完成部署；
- 自定义域名 HTTPS 证书匹配 `www.wyf219.top`；
- 不包含参考站的受版权保护图片、头像或文章内容。

## 12. 明确不做

首版不包含：

- 登录、注册和后台管理；
- 数据库；
- 自建评论服务；
- 实时在线人数或自建访问量计数；
- 构建时实时同步或持续镜像 CSDN；
- 参考站的全屏电影背景欢迎页；
- 直接复制参考站的品牌和素材。

## 13. 后续可选配置

以下内容不会阻止首版部署，可在发布后按需要调整：

- 站点显示名称和一句简介；
- 首页背景图及移动端裁切图；
- 是否公开邮箱；
- 是否公开所在地；
- 公告卡片的初始内容；
- 后续是否继续从外部平台增量迁移文章；
- 是否在首版同时启用 Giscus 评论。

首版已采用：保留现有头像和个人资料，旧主页内容迁移到“关于/项目”，一次性迁移 23 篇 CSDN 旧文并保留原文链接，新增 mini-pi Agent 教程，不公开手机号，不显示访问量，不启用评论。
